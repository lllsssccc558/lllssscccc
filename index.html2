<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>貪食蛇遊戲 - 多種食物挑戰！</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Courier New', monospace;
            background-color: #000;
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow: hidden;
        }
        
        .game-header {
            text-align: center;
            margin: 20px 0;
            padding: 10px;
            width: 100%;
            background-color: #111;
            border-bottom: 3px solid #ff3333;
        }
        
        h1 {
            color: #ff3333;
            font-size: 2.8rem;
            text-shadow: 0 0 15px rgba(255, 50, 50, 0.7);
            letter-spacing: 2px;
            margin-bottom: 10px;
        }
        
        .subtitle {
            color: #4CAF50;
            font-size: 1.2rem;
        }
        
        .game-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .stats-container {
            display: flex;
            justify-content: space-between;
            width: 600px;
            background-color: #111;
            padding: 15px 25px;
            border-radius: 10px;
            border: 2px solid #333;
        }
        
        .score-box, .high-score-box, .length-box, .speed-box {
            text-align: center;
        }
        
        .stat-label {
            color: #aaa;
            font-size: 1rem;
            margin-bottom: 5px;
        }
        
        .stat-value {
            color: #4CAF50;
            font-size: 2.2rem;
            font-weight: bold;
        }
        
        #gameCanvas {
            background-color: #000;
            border: 4px solid #333;
            border-radius: 5px;
            box-shadow: 0 0 25px rgba(255, 50, 50, 0.3);
        }
        
        .controls {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            width: 600px;
        }
        
        .buttons {
            display: flex;
            gap: 15px;
        }
        
        button {
            padding: 12px 30px;
            font-size: 1.1rem;
            font-weight: bold;
            background-color: #333;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.2s;
            min-width: 140px;
            border: 2px solid #444;
        }
        
        button:hover {
            background-color: #444;
            transform: translateY(-2px);
        }
        
        #startBtn {
            background-color: #4CAF50;
            border-color: #5cb85c;
        }
        
        #startBtn:hover {
            background-color: #5cb85c;
        }
        
        #pauseBtn {
            background-color: #f0ad4e;
            border-color: #eea236;
        }
        
        #pauseBtn:hover {
            background-color: #eea236;
        }
        
        #restartBtn {
            background-color: #d9534f;
            border-color: #d43f3a;
        }
        
        #restartBtn:hover {
            background-color: #d43f3a;
        }
        
        .food-types {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 10px;
            flex-wrap: wrap;
        }
        
        .food-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px;
            background-color: #1a1a1a;
            border-radius: 8px;
            border: 1px solid #333;
            min-width: 120px;
        }
        
        .food-icon {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            margin-bottom: 8px;
        }
        
        .normal-food {
            background: linear-gradient(45deg, #ff3333, #ff6666);
            box-shadow: 0 0 10px #ff3333;
        }
        
        .golden-food {
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            box-shadow: 0 0 10px #ffd700;
            animation: golden-glow 2s infinite alternate;
        }
        
        @keyframes golden-glow {
            from { box-shadow: 0 0 10px #ffd700; }
            to { box-shadow: 0 0 20px #ffed4e; }
        }
        
        .poison-food {
            background: linear-gradient(45deg, #9b30ff, #bf7fff);
            box-shadow: 0 0 10px #9b30ff;
            animation: poison-pulse 1.5s infinite alternate;
        }
        
        @keyframes poison-pulse {
            from { box-shadow: 0 0 10px #9b30ff; }
            to { box-shadow: 0 0 15px #bf7fff; }
        }
        
        .food-name {
            color: #fff;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .food-effect {
            color: #ccc;
            font-size: 0.8rem;
            text-align: center;
        }
        
        .instructions {
            background-color: #111;
            padding: 20px;
            border-radius: 10px;
            border: 2px solid #333;
            width: 100%;
            margin-top: 10px;
        }
        
        .instructions h3 {
            color: #ff3333;
            margin-bottom: 15px;
            text-align: center;
            font-size: 1.4rem;
        }
        
        .instructions p {
            margin-bottom: 8px;
            color: #ccc;
            line-height: 1.5;
        }
        
        .key {
            display: inline-block;
            background-color: #222;
            padding: 3px 8px;
            border-radius: 4px;
            border: 1px solid #444;
            font-weight: bold;
            color: #4CAF50;
            margin: 0 3px;
        }
        
        .mobile-controls {
            display: none;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 10px;
            margin-top: 20px;
            width: 300px;
        }
        
        .mobile-controls button {
            min-width: auto;
            padding: 20px;
            font-size: 1.8rem;
            background-color: #333;
        }
        
        .up-btn {
            grid-column: 2;
            grid-row: 1;
        }
        
        .left-btn {
            grid-column: 1;
            grid-row: 2;
        }
        
        .down-btn {
            grid-column: 2;
            grid-row: 2;
        }
        
        .right-btn {
            grid-column: 3;
            grid-row: 2;
        }
        
        .game-over {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(0, 0, 0, 0.95);
            padding: 40px;
            border-radius: 15px;
            text-align: center;
            border: 4px solid #ff3333;
            display: none;
            z-index: 10;
            width: 80%;
            max-width: 500px;
            box-shadow: 0 0 50px rgba(255, 50, 50, 0.5);
        }
        
        .game-over h2 {
            color: #ff3333;
            font-size: 3rem;
            margin-bottom: 25px;
            text-shadow: 0 0 10px rgba(255, 50, 50, 0.7);
        }
        
        .game-over p {
            font-size: 1.5rem;
            margin-bottom: 30px;
            color: #fff;
        }
        
        .game-over span {
            color: #4CAF50;
            font-weight: bold;
            font-size: 2rem;
        }
        
        .effect-notification {
            position: absolute;
            top: 20%;
            left: 50%;
            transform: translateX(-50%);
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 15px 25px;
            border-radius: 10px;
            font-size: 1.2rem;
            font-weight: bold;
            text-align: center;
            z-index: 20;
            display: none;
            border: 2px solid #4CAF50;
            box-shadow: 0 0 15px rgba(76, 175, 80, 0.5);
        }
        
        .apple {
            color: #ff3333;
            font-weight: bold;
        }
        
        .snake {
            color: #4CAF50;
            font-weight: bold;
        }
        
        @media (max-width: 650px) {
            h1 {
                font-size: 2rem;
            }
            
            .stats-container, .controls, #gameCanvas {
                width: 95vw;
            }
            
            #gameCanvas {
                height: auto;
            }
            
            .buttons {
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .mobile-controls {
                display: grid;
            }
            
            .game-over {
                padding: 25px;
            }
            
            .game-over h2 {
                font-size: 2.2rem;
            }
            
            .food-types {
                gap: 10px;
            }
            
            .food-item {
                min-width: 100px;
                padding: 8px;
            }
        }
        
        @media (max-width: 500px) {
            .stats-container {
                flex-wrap: wrap;
                gap: 15px;
                justify-content: center;
            }
            
            button {
                min-width: 120px;
                padding: 10px 20px;
            }
            
            .food-item {
                min-width: 80px;
            }
        }
    </style>
</head>
<body>
    <div class="game-header">
        <h1>貪食蛇遊戲</h1>
        <div class="subtitle">吃掉不同食物獲得特殊效果！</div>
    </div>
    
    <div class="game-container">
        <div class="stats-container">
            <div class="score-box">
                <div class="stat-label">分數</div>
                <div class="stat-value" id="score">0</div>
            </div>
            <div class="length-box">
                <div class="stat-label">蛇長度</div>
                <div class="stat-value" id="snakeLength">3</div>
            </div>
            <div class="speed-box">
                <div class="stat-label">速度</div>
                <div class="stat-value" id="speed">8</div>
            </div>
            <div class="high-score-box">
                <div class="stat-label">最高分</div>
                <div class="stat-value" id="highScore">0</div>
            </div>
        </div>
        
        <canvas id="gameCanvas" width="600" height="400"></canvas>
        
        <div class="food-types">
            <div class="food-item">
                <div class="food-icon normal-food"></div>
                <div class="food-name">普通蘋果</div>
                <div class="food-effect">+10分<br>長度+1</div>
            </div>
            <div class="food-item">
                <div class="food-icon golden-food"></div>
                <div class="food-name">金蘋果</div>
                <div class="food-effect">+30分<br>長度+2</div>
            </div>
            <div class="food-item">
                <div class="food-icon poison-food"></div>
                <div class="food-name">毒蘋果</div>
                <div class="food-effect">+5分<br>速度變快</div>
            </div>
        </div>
        
        <div class="game-over" id="gameOverScreen">
            <h2>遊戲結束！</h2>
            <p>你的分數: <span id="finalScore">0</span></p>
            <p>蛇長度: <span id="finalLength">3</span></p>
            <button id="playAgainBtn">再玩一次</button>
        </div>
        
        <div class="effect-notification" id="effectNotification"></div>
    </div>
    
    <div class="controls">
        <div class="buttons">
            <button id="startBtn">開始遊戲</button>
            <button id="pauseBtn">暫停遊戲</button>
            <button id="restartBtn">重新開始</button>
        </div>
        
        <div class="mobile-controls">
            <button class="up-btn" id="upBtn">↑</button>
            <button class="left-btn" id="leftBtn">←</button>
            <button class="down-btn" id="downBtn">↓</button>
            <button class="right-btn" id="rightBtn">→</button>
        </div>
        
        <div class="instructions">
            <h3>遊戲說明</h3>
            <p>1. 使用 <span class="key">↑</span> <span class="key">↓</span> <span class="key">←</span> <span class="key">→</span> 方向鍵或 <span class="key">W</span> <span class="key">A</span> <span class="key">S</span> <span class="key">D</span> 控制蛇移動</p>
            <p>2. 三種食物效果：</p>
            <p style="margin-left: 20px;">• <span style="color:#ff3333">紅蘋果</span>: +10分，長度+1</p>
            <p style="margin-left: 20px;">• <span style="color:#ffd700">金蘋果</span>: +30分，長度+2，較稀有</p>
            <p style="margin-left: 20px;">• <span style="color:#9b30ff">毒蘋果</span>: +5分，遊戲速度加快</p>
            <p>3. 避免撞到牆壁或自己的身體</p>
            <p>4. 速度會隨分數和吃到毒蘋果而增加</p>
        </div>
    </div>

    <script>
        // 遊戲變數
        const canvas = document.getElementById('gameCanvas');
        const ctx = canvas.getContext('2d');
        const scoreElement = document.getElementById('score');
        const snakeLengthElement = document.getElementById('snakeLength');
        const speedElement = document.getElementById('speed');
        const highScoreElement = document.getElementById('highScore');
        const finalScoreElement = document.getElementById('finalScore');
        const finalLengthElement = document.getElementById('finalLength');
        const gameOverScreen = document.getElementById('gameOverScreen');
        const effectNotification = document.getElementById('effectNotification');
        
        // 按鈕元素
        const startBtn = document.getElementById('startBtn');
        const pauseBtn = document.getElementById('pauseBtn');
        const restartBtn = document.getElementById('restartBtn');
        const playAgainBtn = document.getElementById('playAgainBtn');
        
        // 移動控制按鈕
        const upBtn = document.getElementById('upBtn');
        const downBtn = document.getElementById('downBtn');
        const leftBtn = document.getElementById('leftBtn');
        const rightBtn = document.getElementById('rightBtn');
        
        // 遊戲參數
        const gridSize = 20;
        const tileCountX = canvas.width / gridSize;
        const tileCountY = canvas.height / gridSize;
        
        // 食物類型定義
        const FOOD_TYPES = {
            NORMAL: { 
                name: 'normal', 
                color: '#ff3333', 
                highlight: '#ff6666', 
                score: 10, 
                lengthIncrease: 1,
                spawnChance: 0.7 // 70% 機率
            },
            GOLDEN: { 
                name: 'golden', 
                color: '#ffd700', 
                highlight: '#ffed4e', 
                score: 30, 
                lengthIncrease: 2,
                spawnChance: 0.15 // 15% 機率
            },
            POISON: { 
                name: 'poison', 
                color: '#9b30ff', 
                highlight: '#bf7fff', 
                score: 5, 
                lengthIncrease: 0,
                speedIncrease: 1,
                spawnChance: 0.15 // 15% 機率
            }
        };
        
        let snake = [];
        let food = {x: 0, y: 0, type: FOOD_TYPES.NORMAL};
        let dx = 0;
        let dy = 0;
        let score = 0;
        let snakeLength = 3;
        let highScore = localStorage.getItem('snakeHighScore') || 0;
        let gameRunning = false;
        let gamePaused = false;
        let gameLoop;
        let speed = 8; // 初始速度
        let poisonFoodEaten = 0; // 記錄吃到的毒蘋果數量
        
        // 初始化遊戲
        function initGame() {
            // 初始化蛇 - 放在畫布中央
            snake = [
                {x: Math.floor(tileCountX/2) * gridSize, y: Math.floor(tileCountY/2) * gridSize},
                {x: (Math.floor(tileCountX/2)-1) * gridSize, y: Math.floor(tileCountY/2) * gridSize},
                {x: (Math.floor(tileCountX/2)-2) * gridSize, y: Math.floor(tileCountY/2) * gridSize}
            ];
            
            // 初始化食物
            generateFood();
            
            // 重置方向 (初始向右移動)
            dx = gridSize;
            dy = 0;
            
            // 重置分數和速度
            score = 0;
            snakeLength = 3;
            speed = 8;
            poisonFoodEaten = 0;
            scoreElement.textContent = score;
            snakeLengthElement.textContent = snakeLength;
            speedElement.textContent = speed;
            highScoreElement.textContent = highScore;
            
            // 隱藏遊戲結束畫面
            gameOverScreen.style.display = 'none';
            effectNotification.style.display = 'none';
        }
        
        // 隨機生成食物（包含類型）
        function generateFood() {
            // 隨機選擇食物類型
            const rand = Math.random();
            let selectedType;
            
            if (rand < FOOD_TYPES.NORMAL.spawnChance) {
                selectedType = FOOD_TYPES.NORMAL;
            } else if (rand < FOOD_TYPES.NORMAL.spawnChance + FOOD_TYPES.GOLDEN.spawnChance) {
                selectedType = FOOD_TYPES.GOLDEN;
            } else {
                selectedType = FOOD_TYPES.POISON;
            }
            
            // 隨機生成食物位置
            food = {
                x: Math.floor(Math.random() * tileCountX) * gridSize,
                y: Math.floor(Math.random() * tileCountY) * gridSize,
                type: selectedType
            };
            
            // 確保食物不會出現在蛇身上
            for (let segment of snake) {
                if (segment.x === food.x && segment.y === food.y) {
                    generateFood();
                    return;
                }
            }
        }
        
        // 繪製遊戲
        function drawGame() {
            // 清除畫布
            ctx.fillStyle = '#000';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            // 繪製網格 (輔助線)
            ctx.strokeStyle = '#111';
            ctx.lineWidth = 0.5;
            
            // 繪製垂直網格線
            for (let i = 0; i < tileCountX; i++) {
                ctx.beginPath();
                ctx.moveTo(i * gridSize, 0);
                ctx.lineTo(i * gridSize, canvas.height);
                ctx.stroke();
            }
            
            // 繪製水平網格線
            for (let i = 0; i < tileCountY; i++) {
                ctx.beginPath();
                ctx.moveTo(0, i * gridSize);
                ctx.lineTo(canvas.width, i * gridSize);
                ctx.stroke();
            }
            
            // 繪製蛇
            snake.forEach((segment, index) => {
                // 蛇頭用不同顏色
                if (index === 0) {
                    ctx.fillStyle = '#4CAF50'; // 蛇頭顏色
                } else {
                    // 蛇身用漸變綠色
                    const greenValue = 100 + Math.floor(155 * (index / snake.length));
                    ctx.fillStyle = `rgb(50, ${greenValue}, 80)`;
                }
                
                ctx.fillRect(segment.x, segment.y, gridSize, gridSize);
                
                // 蛇身邊框
                ctx.strokeStyle = '#0a3d0a';
                ctx.lineWidth = 1;
                ctx.strokeRect(segment.x, segment.y, gridSize, gridSize);
                
                // 繪製蛇眼睛 (只有頭部)
                if (index === 0) {
                    ctx.fillStyle = '#fff';
                    
                    // 根據方向確定眼睛位置
                    let leftEyeX, leftEyeY, rightEyeX, rightEyeY;
                    
                    if (dx > 0) { // 向右移動
                        leftEyeX = segment.x + gridSize - 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else if (dx < 0) { // 向左移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else if (dy > 0) { // 向下移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + gridSize - 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else { // 向上移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + 6;
                    }
                    
                    // 繪製眼睛
                    ctx.beginPath();
                    ctx.arc(leftEyeX, leftEyeY, 2, 0, Math.PI * 2);
                    ctx.fill();
                    
                    ctx.beginPath();
                    ctx.arc(rightEyeX, rightEyeY, 2, 0, Math.PI * 2);
                    ctx.fill();
                }
            });
            
            // 繪製食物（根據類型）
            const foodType = food.type;
            
            // 食物主體
            ctx.fillStyle = foodType.color;
            ctx.beginPath();
            ctx.arc(food.x + gridSize/2, food.y + gridSize/2, gridSize/2 - 1, 0, Math.PI * 2);
            ctx.fill();
            
            // 食物高光效果
            ctx.fillStyle = foodType.highlight;
            ctx.beginPath();
            ctx.arc(food.x + gridSize/3, food.y + gridSize/3, gridSize/6, 0, Math.PI * 2);
            ctx.fill();
            
            // 食物梗
            ctx.fillStyle = '#8B4513';
            ctx.fillRect(food.x + gridSize/2 - 1, food.y - 2, 2, 5);
            
            // 食物葉子
            ctx.fillStyle = '#228B22';
            ctx.beginPath();
            ctx.ellipse(food.x + gridSize/2 + 3, food.y + 2, 3, 2, Math.PI/4, 0, Math.PI * 2);
            ctx.fill();
            
            // 如果是金蘋果，添加特殊光芒效果
            if (foodType.name === 'golden') {
                ctx.strokeStyle = 'rgba(255, 215, 0, 0.5)';
                ctx.lineWidth = 2;
                ctx.beginPath();
                ctx.arc(food.x + gridSize/2, food.y + gridSize/2, gridSize/2 + 2, 0, Math.PI * 2);
                ctx.stroke();
            }
            
            // 如果是毒蘋果，添加特殊紋理
            if (foodType.name === 'poison') {
                ctx.fillStyle = 'rgba(255, 255, 255, 0.3)';
                ctx.beginPath();
                ctx.arc(food.x + gridSize/2 - 3, food.y + gridSize/2 - 3, 2, 0, Math.PI * 2);
                ctx.fill();
                ctx.beginPath();
                ctx.arc(food.x + gridSize/2 + 3, food.y + gridSize/2 + 2, 2, 0, Math.PI * 2);
                ctx.fill();
            }
        }
        
        // 顯示效果通知
        function showEffectNotification(message, color) {
            effectNotification.textContent = message;
            effectNotification.style.color = color;
            effectNotification.style.borderColor = color;
            effectNotification.style.boxShadow = `0 0 15px ${color}80`;
            effectNotification.style.display = 'block';
            
            // 3秒後隱藏通知
            setTimeout(() => {
                effectNotification.style.display = 'none';
            }, 3000);
        }
        
        // 閃光效果 (當吃到食物時)
        function flashEffect(color) {
            // 暫存原始背景色
            const originalFillStyle = ctx.fillStyle;
            
            // 根據食物類型使用不同顏色閃光
            const flashColor = color || 'rgba(76, 175, 80, 0.3)';
            
            // 短暫改變畫布背景色
            ctx.fillStyle = flashColor;
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            // 恢復原始背景色
            setTimeout(() => {
                ctx.fillStyle = originalFillStyle;
            }, 100);
        }
        
        // 更新遊戲狀態
        function updateGame() {
            if (!gameRunning || gamePaused) return;
            
            // 移動蛇
            const head = {x: snake[0].x + dx, y: snake[0].y + dy};
            
            // 檢查是否撞牆
            if (head.x < 0 || head.x >= canvas.width || head.y < 0 || head.y >= canvas.height) {
                gameOver();
                return;
            }
            
            // 檢查是否撞到自己
            for (let segment of snake) {
                if (head.x === segment.x && head.y === segment.y) {
                    gameOver();
                    return;
                }
            }
            
            // 添加新的頭部
            snake.unshift(head);
            
            // 檢查是否吃到食物
            if (head.x === food.x && head.y === food.y) {
                const foodType = food.type;
                
                // 增加分數
                score += foodType.score;
                scoreElement.textContent = score;
                
                // 蛇長度增加
                snakeLength += foodType.lengthIncrease;
                snakeLengthElement.textContent = snakeLength;
                
                // 特殊效果處理
                if (foodType.name === 'poison') {
                    // 毒蘋果：增加速度
                    poisonFoodEaten++;
                    speed += foodType.speedIncrease;
                    speedElement.textContent = speed;
                    
                    // 重新設置遊戲循環以應用新速度
                    clearInterval(gameLoop);
                    gameLoop = setInterval(gameStep, 1000 / speed);
                    
                    // 顯示通知
                    showEffectNotification('速度加快！', '#9b30ff');
                    flashEffect('rgba(155, 48, 255, 0.3)');
                } 
                else if (foodType.name === 'golden') {
                    // 金蘋果：額外獎勵
                    showEffectNotification('金蘋果！+30分', '#ffd700');
                    flashEffect('rgba(255, 215, 0, 0.3)');
                }
                else {
                    // 普通蘋果
                    flashEffect('rgba(255, 50, 50, 0.3)');
                }
                
                // 每得50分增加速度（除了毒蘋果之外的速度增加）
                if (score % 50 === 0 && foodType.name !== 'poison') {
                    speed += 1;
                    speedElement.textContent = speed;
                    clearInterval(gameLoop);
                    gameLoop = setInterval(gameStep, 1000 / speed);
                    
                    showEffectNotification('速度等級提升！', '#4CAF50');
                }
                
                // 生成新食物
                generateFood();
            } else {
                // 如果沒吃到食物，移除尾部
                snake.pop();
            }
        }
        
        // 遊戲主循環
        function gameStep() {
            updateGame();
            drawGame();
        }
        
        // 開始遊戲循環
        function startGame() {
            if (!gameRunning) {
                initGame();
                gameRunning = true;
                gamePaused = false;
                startBtn.textContent = "重新開始";
                pauseBtn.textContent = "暫停遊戲";
                
                // 清除現有循環
                if (gameLoop) {
                    clearInterval(gameLoop);
                }
                
                // 開始新循環
                gameLoop = setInterval(gameStep, 1000 / speed);
            }
        }
        
        // 暫停/繼續遊戲
        function togglePause() {
            if (!gameRunning) return;
            
            gamePaused = !gamePaused;
            pauseBtn.textContent = gamePaused ? "繼續遊戲" : "暫停遊戲";
            
            if (gamePaused) {
                clearInterval(gameLoop);
            } else {
                gameLoop = setInterval(gameStep, 1000 / speed);
            }
        }
        
        // 重新開始遊戲
        function restartGame() {
            clearInterval(gameLoop);
            initGame();
            gameRunning = true;
            gamePaused = false;
            pauseBtn.textContent = "暫停遊戲";
            gameLoop = setInterval(gameStep, 1000 / speed);
        }
        
        // 遊戲結束
        function gameOver() {
            gameRunning = false;
            clearInterval(gameLoop);
            
            // 更新最高分數
            if (score > highScore) {
                highScore = score;
                localStorage.setItem('snakeHighScore', highScore);
                highScoreElement.textContent = highScore;
                
                // 顯示打破紀錄的通知
                showEffectNotification('新紀錄！', '#ffd700');
            }
            
            // 顯示遊戲結束畫面
            finalScoreElement.textContent = score;
            finalLengthElement.textContent = snakeLength;
            gameOverScreen.style.display = 'block';
        }
        
        // 控制蛇的移動方向
        function changeDirection(newDx, newDy) {
            // 防止直接反向移動 (例如不能從向右直接變向左)
            if ((dx !== 0 && newDx === -dx) || (dy !== 0 && newDy === -dy)) {
                return;
            }
            
            dx = newDx;
            dy = newDy;
        }
        
        // 鍵盤控制
        document.addEventListener('keydown', (e) => {
            if (!gameRunning || gamePaused) return;
            
            switch(e.key) {
                case 'ArrowUp':
                case 'w':
                case 'W':
                    changeDirection(0, -gridSize);
                    break;
                case 'ArrowDown':
                case 's':
                case 'S':
                    changeDirection(0, gridSize);
                    break;
                case 'ArrowLeft':
                case 'a':
                case 'A':
                    changeDirection(-gridSize, 0);
                    break;
                case 'ArrowRight':
                case 'd':
                case 'D':
                    changeDirection(gridSize, 0);
                    break;
                case ' ': // 空格鍵暫停/繼續
                    togglePause();
                    break;
            }
        });
        
        // 按鈕事件監聽
        startBtn.addEventListener('click', startGame);
        pauseBtn.addEventListener('click', togglePause);
        restartBtn.addEventListener('click', restartGame);
        playAgainBtn.addEventListener('click', restartGame);
        
        // 移動控制按鈕事件
        upBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(0, -gridSize);
        });
        downBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(0, gridSize);
        });
        leftBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(-gridSize, 0);
        });
        rightBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(gridSize, 0);
        });
        
        // 初始化遊戲顯示
        initGame();
        drawGame();
        highScoreElement.textContent = highScore;
        
        // 提示用戶如何開始遊戲
        setTimeout(() => {
            if (!gameRunning) {
                alert("歡迎來到貪食蛇遊戲 - 多種食物版本！\n\n點擊「開始遊戲」按鈕開始遊玩！\n\n使用方向鍵或WASD控制蛇的移動。\n\n三種食物類型：\n• 紅蘋果：+10分，長度+1\n• 金蘋果：+30分，長度+2\n• 毒蘋果：+5分，遊戲速度加快\n\n避免撞牆或撞到自己！");
            }
        }, 300);
    </script>
</body>
</html>
