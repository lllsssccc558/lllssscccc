<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>貪食蛇遊戲 - 吃掉紅色蘋果變長！</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Courier New', monospace;
            background-color: #000;
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow: hidden;
        }
        
        .game-header {
            text-align: center;
            margin: 20px 0;
            padding: 10px;
            width: 100%;
            background-color: #111;
            border-bottom: 3px solid #ff3333;
        }
        
        h1 {
            color: #ff3333;
            font-size: 2.8rem;
            text-shadow: 0 0 15px rgba(255, 50, 50, 0.7);
            letter-spacing: 2px;
            margin-bottom: 10px;
        }
        
        .subtitle {
            color: #4CAF50;
            font-size: 1.2rem;
        }
        
        .game-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .stats-container {
            display: flex;
            justify-content: space-between;
            width: 600px;
            background-color: #111;
            padding: 15px 25px;
            border-radius: 10px;
            border: 2px solid #333;
        }
        
        .score-box, .high-score-box, .length-box {
            text-align: center;
        }
        
        .stat-label {
            color: #aaa;
            font-size: 1rem;
            margin-bottom: 5px;
        }
        
        .stat-value {
            color: #4CAF50;
            font-size: 2.2rem;
            font-weight: bold;
        }
        
        #gameCanvas {
            background-color: #000;
            border: 4px solid #333;
            border-radius: 5px;
            box-shadow: 0 0 25px rgba(255, 50, 50, 0.3);
        }
        
        .controls {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            width: 600px;
        }
        
        .buttons {
            display: flex;
            gap: 15px;
        }
        
        button {
            padding: 12px 30px;
            font-size: 1.1rem;
            font-weight: bold;
            background-color: #333;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.2s;
            min-width: 140px;
            border: 2px solid #444;
        }
        
        button:hover {
            background-color: #444;
            transform: translateY(-2px);
        }
        
        #startBtn {
            background-color: #4CAF50;
            border-color: #5cb85c;
        }
        
        #startBtn:hover {
            background-color: #5cb85c;
        }
        
        #pauseBtn {
            background-color: #f0ad4e;
            border-color: #eea236;
        }
        
        #pauseBtn:hover {
            background-color: #eea236;
        }
        
        #restartBtn {
            background-color: #d9534f;
            border-color: #d43f3a;
        }
        
        #restartBtn:hover {
            background-color: #d43f3a;
        }
        
        .instructions {
            background-color: #111;
            padding: 20px;
            border-radius: 10px;
            border: 2px solid #333;
            width: 100%;
            margin-top: 10px;
        }
        
        .instructions h3 {
            color: #ff3333;
            margin-bottom: 15px;
            text-align: center;
            font-size: 1.4rem;
        }
        
        .instructions p {
            margin-bottom: 8px;
            color: #ccc;
            line-height: 1.5;
        }
        
        .key {
            display: inline-block;
            background-color: #222;
            padding: 3px 8px;
            border-radius: 4px;
            border: 1px solid #444;
            font-weight: bold;
            color: #4CAF50;
            margin: 0 3px;
        }
        
        .mobile-controls {
            display: none;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 10px;
            margin-top: 20px;
            width: 300px;
        }
        
        .mobile-controls button {
            min-width: auto;
            padding: 20px;
            font-size: 1.8rem;
            background-color: #333;
        }
        
        .up-btn {
            grid-column: 2;
            grid-row: 1;
        }
        
        .left-btn {
            grid-column: 1;
            grid-row: 2;
        }
        
        .down-btn {
            grid-column: 2;
            grid-row: 2;
        }
        
        .right-btn {
            grid-column: 3;
            grid-row: 2;
        }
        
        .game-over {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(0, 0, 0, 0.95);
            padding: 40px;
            border-radius: 15px;
            text-align: center;
            border: 4px solid #ff3333;
            display: none;
            z-index: 10;
            width: 80%;
            max-width: 500px;
            box-shadow: 0 0 50px rgba(255, 50, 50, 0.5);
        }
        
        .game-over h2 {
            color: #ff3333;
            font-size: 3rem;
            margin-bottom: 25px;
            text-shadow: 0 0 10px rgba(255, 50, 50, 0.7);
        }
        
        .game-over p {
            font-size: 1.5rem;
            margin-bottom: 30px;
            color: #fff;
        }
        
        .game-over span {
            color: #4CAF50;
            font-weight: bold;
            font-size: 2rem;
        }
        
        .apple {
            color: #ff3333;
            font-weight: bold;
        }
        
        .snake {
            color: #4CAF50;
            font-weight: bold;
        }
        
        @media (max-width: 650px) {
            h1 {
                font-size: 2rem;
            }
            
            .stats-container, .controls, #gameCanvas {
                width: 95vw;
            }
            
            #gameCanvas {
                height: auto;
            }
            
            .buttons {
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .mobile-controls {
                display: grid;
            }
            
            .game-over {
                padding: 25px;
            }
            
            .game-over h2 {
                font-size: 2.2rem;
            }
        }
        
        @media (max-width: 500px) {
            .stats-container {
                flex-direction: column;
                gap: 15px;
            }
            
            button {
                min-width: 120px;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="game-header">
        <h1>貪食蛇遊戲</h1>
        <div class="subtitle">吃掉<span class="apple">紅色蘋果</span>讓<span class="snake">蛇</span>變長！</div>
    </div>
    
    <div class="game-container">
        <div class="stats-container">
            <div class="score-box">
                <div class="stat-label">分數</div>
                <div class="stat-value" id="score">0</div>
            </div>
            <div class="length-box">
                <div class="stat-label">蛇長度</div>
                <div class="stat-value" id="snakeLength">3</div>
            </div>
            <div class="high-score-box">
                <div class="stat-label">最高分</div>
                <div class="stat-value" id="highScore">0</div>
            </div>
        </div>
        
        <canvas id="gameCanvas" width="600" height="400"></canvas>
        
        <div class="game-over" id="gameOverScreen">
            <h2>遊戲結束！</h2>
            <p>你的分數: <span id="finalScore">0</span></p>
            <p>蛇長度: <span id="finalLength">3</span></p>
            <button id="playAgainBtn">再玩一次</button>
        </div>
    </div>
    
    <div class="controls">
        <div class="buttons">
            <button id="startBtn">開始遊戲</button>
            <button id="pauseBtn">暫停遊戲</button>
            <button id="restartBtn">重新開始</button>
        </div>
        
        <div class="mobile-controls">
            <button class="up-btn" id="upBtn">↑</button>
            <button class="left-btn" id="leftBtn">←</button>
            <button class="down-btn" id="downBtn">↓</button>
            <button class="right-btn" id="rightBtn">→</button>
        </div>
        
        <div class="instructions">
            <h3>遊戲說明</h3>
            <p>1. 使用 <span class="key">↑</span> <span class="key">↓</span> <span class="key">←</span> <span class="key">→</span> 方向鍵或 <span class="key">W</span> <span class="key">A</span> <span class="key">S</span> <span class="key">D</span> 控制蛇移動</p>
            <p>2. 吃到 <span class="apple">紅色蘋果</span> 可以增加 <span class="snake">10分</span> 並讓 <span class="snake">蛇變長</span>！</p>
            <p>3. 避免撞到牆壁或自己的身體</p>
            <p>4. 蛇移動速度會隨分數增加而加快</p>
        </div>
    </div>

    <script>
        // 遊戲變數
        const canvas = document.getElementById('gameCanvas');
        const ctx = canvas.getContext('2d');
        const scoreElement = document.getElementById('score');
        const snakeLengthElement = document.getElementById('snakeLength');
        const highScoreElement = document.getElementById('highScore');
        const finalScoreElement = document.getElementById('finalScore');
        const finalLengthElement = document.getElementById('finalLength');
        const gameOverScreen = document.getElementById('gameOverScreen');
        
        // 按鈕元素
        const startBtn = document.getElementById('startBtn');
        const pauseBtn = document.getElementById('pauseBtn');
        const restartBtn = document.getElementById('restartBtn');
        const playAgainBtn = document.getElementById('playAgainBtn');
        
        // 移動控制按鈕
        const upBtn = document.getElementById('upBtn');
        const downBtn = document.getElementById('downBtn');
        const leftBtn = document.getElementById('leftBtn');
        const rightBtn = document.getElementById('rightBtn');
        
        // 遊戲參數
        const gridSize = 20;
        const tileCountX = canvas.width / gridSize;
        const tileCountY = canvas.height / gridSize;
        
        let snake = [];
        let food = {x: 0, y: 0};
        let dx = 0;
        let dy = 0;
        let score = 0;
        let snakeLength = 3;
        let highScore = localStorage.getItem('snakeHighScore') || 0;
        let gameRunning = false;
        let gamePaused = false;
        let gameLoop;
        let speed = 8; // 初始速度
        
        // 初始化遊戲
        function initGame() {
            // 初始化蛇 - 放在畫布中央
            snake = [
                {x: Math.floor(tileCountX/2) * gridSize, y: Math.floor(tileCountY/2) * gridSize},
                {x: (Math.floor(tileCountX/2)-1) * gridSize, y: Math.floor(tileCountY/2) * gridSize},
                {x: (Math.floor(tileCountX/2)-2) * gridSize, y: Math.floor(tileCountY/2) * gridSize}
            ];
            
            // 初始化食物
            generateFood();
            
            // 重置方向 (初始向右移動)
            dx = gridSize;
            dy = 0;
            
            // 重置分數和速度
            score = 0;
            snakeLength = 3;
            speed = 8;
            scoreElement.textContent = score;
            snakeLengthElement.textContent = snakeLength;
            highScoreElement.textContent = highScore;
            
            // 隱藏遊戲結束畫面
            gameOverScreen.style.display = 'none';
        }
        
        // 生成食物
        function generateFood() {
            // 隨機生成食物位置
            food = {
                x: Math.floor(Math.random() * tileCountX) * gridSize,
                y: Math.floor(Math.random() * tileCountY) * gridSize
            };
            
            // 確保食物不會出現在蛇身上
            for (let segment of snake) {
                if (segment.x === food.x && segment.y === food.y) {
                    generateFood();
                    return;
                }
            }
        }
        
        // 繪製遊戲
        function drawGame() {
            // 清除畫布
            ctx.fillStyle = '#000';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            // 繪製網格 (輔助線)
            ctx.strokeStyle = '#111';
            ctx.lineWidth = 0.5;
            
            // 繪製垂直網格線
            for (let i = 0; i < tileCountX; i++) {
                ctx.beginPath();
                ctx.moveTo(i * gridSize, 0);
                ctx.lineTo(i * gridSize, canvas.height);
                ctx.stroke();
            }
            
            // 繪製水平網格線
            for (let i = 0; i < tileCountY; i++) {
                ctx.beginPath();
                ctx.moveTo(0, i * gridSize);
                ctx.lineTo(canvas.width, i * gridSize);
                ctx.stroke();
            }
            
            // 繪製蛇
            snake.forEach((segment, index) => {
                // 蛇頭用不同顏色
                if (index === 0) {
                    ctx.fillStyle = '#4CAF50'; // 蛇頭顏色
                } else {
                    // 蛇身用漸變綠色
                    const greenValue = 100 + Math.floor(155 * (index / snake.length));
                    ctx.fillStyle = `rgb(50, ${greenValue}, 80)`;
                }
                
                ctx.fillRect(segment.x, segment.y, gridSize, gridSize);
                
                // 蛇身邊框
                ctx.strokeStyle = '#0a3d0a';
                ctx.lineWidth = 1;
                ctx.strokeRect(segment.x, segment.y, gridSize, gridSize);
                
                // 繪製蛇眼睛 (只有頭部)
                if (index === 0) {
                    ctx.fillStyle = '#fff';
                    
                    // 根據方向確定眼睛位置
                    let leftEyeX, leftEyeY, rightEyeX, rightEyeY;
                    
                    if (dx > 0) { // 向右移動
                        leftEyeX = segment.x + gridSize - 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else if (dx < 0) { // 向左移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else if (dy > 0) { // 向下移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + gridSize - 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + gridSize - 6;
                    } else { // 向上移動
                        leftEyeX = segment.x + 6;
                        leftEyeY = segment.y + 6;
                        rightEyeX = segment.x + gridSize - 6;
                        rightEyeY = segment.y + 6;
                    }
                    
                    // 繪製眼睛
                    ctx.beginPath();
                    ctx.arc(leftEyeX, leftEyeY, 2, 0, Math.PI * 2);
                    ctx.fill();
                    
                    ctx.beginPath();
                    ctx.arc(rightEyeX, rightEyeY, 2, 0, Math.PI * 2);
                    ctx.fill();
                }
            });
            
            // 繪製食物 (紅色蘋果)
            ctx.fillStyle = '#ff3333';
            ctx.beginPath();
            ctx.arc(food.x + gridSize/2, food.y + gridSize/2, gridSize/2 - 1, 0, Math.PI * 2);
            ctx.fill();
            
            // 蘋果高光效果
            ctx.fillStyle = '#ff6666';
            ctx.beginPath();
            ctx.arc(food.x + gridSize/3, food.y + gridSize/3, gridSize/6, 0, Math.PI * 2);
            ctx.fill();
            
            // 蘋果梗
            ctx.fillStyle = '#8B4513';
            ctx.fillRect(food.x + gridSize/2 - 1, food.y - 2, 2, 5);
            
            // 蘋果葉子
            ctx.fillStyle = '#228B22';
            ctx.beginPath();
            ctx.ellipse(food.x + gridSize/2 + 3, food.y + 2, 3, 2, Math.PI/4, 0, Math.PI * 2);
            ctx.fill();
        }
        
        // 更新遊戲狀態
        function updateGame() {
            if (!gameRunning || gamePaused) return;
            
            // 移動蛇
            const head = {x: snake[0].x + dx, y: snake[0].y + dy};
            
            // 檢查是否撞牆
            if (head.x < 0 || head.x >= canvas.width || head.y < 0 || head.y >= canvas.height) {
                gameOver();
                return;
            }
            
            // 檢查是否撞到自己
            for (let segment of snake) {
                if (head.x === segment.x && head.y === segment.y) {
                    gameOver();
                    return;
                }
            }
            
            // 添加新的頭部
            snake.unshift(head);
            
            // 檢查是否吃到食物
            if (head.x === food.x && head.y === food.y) {
                // 增加分數
                score += 10;
                scoreElement.textContent = score;
                
                // 蛇長度增加
                snakeLength++;
                snakeLengthElement.textContent = snakeLength;
                
                // 每得50分增加速度
                if (score % 50 === 0) {
                    speed += 1;
                    clearInterval(gameLoop);
                    gameLoop = setInterval(gameStep, 1000 / speed);
                }
                
                // 生成新食物
                generateFood();
                
                // 播放吃到食物的音效 (視覺效果)
                flashEffect();
            } else {
                // 如果沒吃到食物，移除尾部
                snake.pop();
            }
        }
        
        // 閃光效果 (當吃到食物時)
        function flashEffect() {
            // 暫存原始背景色
            const originalFillStyle = ctx.fillStyle;
            
            // 短暫改變畫布背景色
            ctx.fillStyle = 'rgba(76, 175, 80, 0.3)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            // 恢復原始背景色
            setTimeout(() => {
                ctx.fillStyle = originalFillStyle;
            }, 100);
        }
        
        // 遊戲主循環
        function gameStep() {
            updateGame();
            drawGame();
        }
        
        // 開始遊戲循環
        function startGame() {
            if (!gameRunning) {
                initGame();
                gameRunning = true;
                gamePaused = false;
                startBtn.textContent = "重新開始";
                pauseBtn.textContent = "暫停遊戲";
                
                // 清除現有循環
                if (gameLoop) {
                    clearInterval(gameLoop);
                }
                
                // 開始新循環
                gameLoop = setInterval(gameStep, 1000 / speed);
            }
        }
        
        // 暫停/繼續遊戲
        function togglePause() {
            if (!gameRunning) return;
            
            gamePaused = !gamePaused;
            pauseBtn.textContent = gamePaused ? "繼續遊戲" : "暫停遊戲";
            
            if (gamePaused) {
                clearInterval(gameLoop);
            } else {
                gameLoop = setInterval(gameStep, 1000 / speed);
            }
        }
        
        // 重新開始遊戲
        function restartGame() {
            clearInterval(gameLoop);
            initGame();
            gameRunning = true;
            gamePaused = false;
            pauseBtn.textContent = "暫停遊戲";
            gameLoop = setInterval(gameStep, 1000 / speed);
        }
        
        // 遊戲結束
        function gameOver() {
            gameRunning = false;
            clearInterval(gameLoop);
            
            // 更新最高分數
            if (score > highScore) {
                highScore = score;
                localStorage.setItem('snakeHighScore', highScore);
                highScoreElement.textContent = highScore;
            }
            
            // 顯示遊戲結束畫面
            finalScoreElement.textContent = score;
            finalLengthElement.textContent = snakeLength;
            gameOverScreen.style.display = 'block';
        }
        
        // 控制蛇的移動方向
        function changeDirection(newDx, newDy) {
            // 防止直接反向移動 (例如不能從向右直接變向左)
            if ((dx !== 0 && newDx === -dx) || (dy !== 0 && newDy === -dy)) {
                return;
            }
            
            dx = newDx;
            dy = newDy;
        }
        
        // 鍵盤控制
        document.addEventListener('keydown', (e) => {
            if (!gameRunning || gamePaused) return;
            
            switch(e.key) {
                case 'ArrowUp':
                case 'w':
                case 'W':
                    changeDirection(0, -gridSize);
                    break;
                case 'ArrowDown':
                case 's':
                case 'S':
                    changeDirection(0, gridSize);
                    break;
                case 'ArrowLeft':
                case 'a':
                case 'A':
                    changeDirection(-gridSize, 0);
                    break;
                case 'ArrowRight':
                case 'd':
                case 'D':
                    changeDirection(gridSize, 0);
                    break;
                case ' ': // 空格鍵暫停/繼續
                    togglePause();
                    break;
            }
        });
        
        // 按鈕事件監聽
        startBtn.addEventListener('click', startGame);
        pauseBtn.addEventListener('click', togglePause);
        restartBtn.addEventListener('click', restartGame);
        playAgainBtn.addEventListener('click', restartGame);
        
        // 移動控制按鈕事件
        upBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(0, -gridSize);
        });
        downBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(0, gridSize);
        });
        leftBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(-gridSize, 0);
        });
        rightBtn.addEventListener('click', () => {
            if (gameRunning && !gamePaused) changeDirection(gridSize, 0);
        });
        
        // 初始化遊戲顯示
        initGame();
        drawGame();
        highScoreElement.textContent = highScore;
        
        // 提示用戶如何開始遊戲
        setTimeout(() => {
            if (!gameRunning) {
                alert("歡迎來到貪食蛇遊戲！\n\n點擊「開始遊戲」按鈕開始遊玩！\n\n使用方向鍵或WASD控制蛇的移動。\n\n目標：吃掉紅色蘋果讓蛇變長，避免撞牆或撞到自己！");
            }
        }, 300);
    </script>
</body>
</html>